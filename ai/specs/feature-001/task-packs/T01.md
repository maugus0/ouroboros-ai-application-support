# Task Pack: T01 — Prompt Lint Tests

## Feature
`feature-001` — LLMOps CI/CD Pipeline

## Status
`done`

---

## Context

### Read these files first
- `ai/specs/feature-001/spec.md` — what we're building
- `ai/specs/feature-001/plan.md` — how we're building it
- `app/llm/prompts.py` — prompt builder functions
- `app/llm/sop_prompt_bundle.py` — SOP prompt file constants
- `app/utils/prompt_utils.py` — build_prompt_json / build_prompt_text helpers
- `requirements-dev.txt` — current dev dependencies

### Relevant existing code
- `app/llm/openai_client.py` — knows the model name to get token limit (e.g. gpt-4o → 128k)
- `app/llm/anthropic_client.py` — knows the model name to get token limit (claude-3-5-sonnet → 200k)
- `tests/unit/` — see the style and pattern of existing unit tests

---

## Objective
Create `tests/llm/test_prompt_lint.py` to check all existing prompt files — **no API key required**, runnable locally and in CI.

## Scope
- [ ] Create `tests/llm/__init__.py` (empty)
- [ ] Create `tests/llm/conftest.py` with:
  - `mock_sop_context` fixture — dict with all keys to inject into SOP prompts
  - `mock_cover_letter_context` fixture — dict with all keys for cover letter
  - `all_prompt_files` fixture — list all `.json` files in `prompts/` directory
- [ ] Create `tests/llm/test_prompt_lint.py` with test cases:
  - `test_all_prompt_json_files_are_valid_json` — load each file, assert no throw
  - `test_sop_outline_prompt_renders_without_error` — call `get_sop_outline_prompt(context=mock_sop_context)`
  - `test_sop_expansion_prompt_renders_without_error`
  - `test_sop_quality_review_prompt_renders_without_error`
  - `test_cover_letter_prompt_renders_without_error`
  - `test_prompt_token_count_within_limit` — use `tiktoken` estimate, assert < 80% model limit
  - `test_no_unfilled_placeholder_in_rendered_prompt` — check no `{{` or `}}` remains after render
- [ ] Add `tiktoken>=0.7` to `requirements-dev.txt`
- [ ] Verify: `pytest tests/llm/test_prompt_lint.py -v` runs pass without env vars

## Out of Scope
- Do not call real LLM API
- Do not create golden fixtures (T02 does this)
- Do not modify any files in `app/`
- Do not modify `deploy.yml` (T04 does this)

## Acceptance Criteria
- [ ] `pytest tests/llm/test_prompt_lint.py -v` passes locally with `ALLOW_DB_FAILURE=true`
- [ ] Test fails when adding `{{unfilled}}` to mock prompt (negative test)
- [ ] Token count test fails when mock returns prompt > 80% limit (negative test)
- [ ] No imports from `app/` require database connection for tests to run
- [ ] All lint/typecheck/test pass: `black`, `isort`, `flake8`, `pylint`, `mypy`

## Constraints
- Do not add dependencies other than `tiktoken` to `requirements-dev.txt`
- Do not modify any files in `app/`
- Do not modify existing test files in `tests/unit/` or `tests/integration/`
- Use the same style fixtures as `tests/unit/conftest.py` if available

---

## Verification Commands
```bash
# Must run from the application-support/ directory
ALLOW_DB_FAILURE=true pytest tests/llm/test_prompt_lint.py -v --tb=short

# Check format
black --check app/ tests/
isort --check-only app/ tests/

# Check lint
flake8 tests/llm/ --max-line-length=120
pylint tests/llm/ --max-line-length=120
```

## Handoff
When finished, update task status in `ai/specs/feature-001/tasks.md` to `review`.