# Task Pack: T02 — Demographic Variants in Golden Fixtures

## Feature
`feature-002` — AI Governance Layer

## Status
`done`

---

## Context

### Read these files first
- `ai/specs/feature-002/spec.md` — section 4 (Bias Detection expected behavior)
- `ai/specs/feature-002/plan.md` — section 3 (Schema changes for golden cases)
- `tests/llm/fixtures/sop_golden_cases.json` — current golden cases
- `tests/llm/fixtures/cover_letter_golden.json` — current cover letter cases

---

## Objective
Expand each golden case in the fixture files with the field `demographic_variants` containing name/gender variants for bias testing in T03.

---

## Scope

- [ ] Modify `tests/llm/fixtures/sop_golden_cases.json`: add `demographic_variants` for each case
  - Each case must have at least 2 variants: 1 male name + 1 female name (same surname or same background)
  - Example: `sop-stem-phd` has original `"Alan Turing"` → variants: `[{"student_name": "Ada Turing"}, {"student_name": "Li Wei"}]`
- [ ] Modify `tests/llm/fixtures/cover_letter_golden.json`: similarly
- [ ] Ensure there is no real PII in any variant (use fictional names)
- [ ] Verify `python3 -m json.tool` passes for both files after modification

---

## Acceptance Criteria
- [ ] Each golden case has the field `demographic_variants` with at least 2 items
- [ ] Each variant has at least the field `student_name` (SOP) or `applicant_name`/`student_name` (Cover Letter)
- [ ] All names in variants are fictional (not real customer names)
- [ ] JSON syntax is valid after modification
- [ ] `python scripts/check_pii.py --path tests/llm/fixtures/` still passes after changes

---

## Constraints
- Do not add more than 3 variants per case to avoid excessive API calls in T03
- Variants only swap `student_name` — do not change experience or qualifications
- Selected names must represent diversity: at least 1 Asian name, 1 Western name, and 1 name of a different gender

---

## Handoff
Update `ai/specs/feature-002/tasks.md` → `review` when done.