# Task Pack: T06 — CI Integration for Feature-002

## Feature
`feature-002` — AI Governance Layer

## Status
`done`

---

## Context

### Read these files first
- `ai/specs/feature-002/spec.md` — all
- `ai/specs/feature-002/plan.md` — section 2 (Data flows)
- `.github/workflows/deploy.yml` — file to be modified
- Task packs T01–T05 to know what has been built

---

## Objective
Consolidate all changes from T01–T05 into `.github/workflows/deploy.yml` so that CI/CD fully reflects the AI Governance Layer.

---

## Scope

- [ ] Add job `pii-guard` (from T01):
  - Trigger when PR has changes in `tests/llm/fixtures/**`
  - `needs: [format-check, lint-check, unit-tests]`
  - Add to `docker-build` needs if triggered (use `result != 'skipped'` pattern)
  - Add to `reports-summary` needs

- [ ] Update job `llm-eval` (from T03, T04, T05):
  - Add env vars: `BIAS_THRESHOLD`, `BIAS_HARD_LIMIT`
  - After step `Run LLM evaluations`: add step `Push eval history` calling `scripts/push_eval_history.py`
  - Update comments in the file to note the new AI Governance features

- [ ] Update `reports-summary`:
  - Add `pii-guard` to needs list
  - Add summary line: `"- PII Guard: ${{ needs.pii-guard.result }}"`

---

## Acceptance Criteria
- [ ] `pii-guard` job appears in GitHub Actions graph
- [ ] `llm-eval` job has `BIAS_THRESHOLD` and `BIAS_HARD_LIMIT` env vars
- [ ] `reports-summary` outputs the result of `pii-guard`
- [ ] YAML file passes syntax check: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"`

---

## Constraints
- Do not modify the logic of existing jobs (format-check, lint-check, unit-tests, docker-build, trivy-scan)
- `pii-guard` must not block `docker-build` when it is skipped (only block when it fails)
- Keep all explanatory comments in the file

---

## Verification Commands
```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml')); print('YAML valid')"
```

## Handoff
Update `ai/specs/feature-002/tasks.md` → `review` when done.
Create PR, link to feature-002 spec in PR description.